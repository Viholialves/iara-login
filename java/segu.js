// Função para voltar à página anterior
function goBack() {
    window.history.back();
}

// Função para ativar autenticação de dois fatores (simulação)
function activateTwoFactor() {
    alert('Autenticação de Dois Fatores (2FA) ativada com sucesso!');
    // Adicione a lógica para ativar 2FA aqui, como redirecionar para configurações de segurança.
}

// Função para redefinir senha (simulação)
function resetPassword() {
    alert('Redefinição de senha iniciada. Verifique seu e-mail.');
    // Adicione a lógica para redefinir a senha aqui, como redirecionar para o fluxo de redefinição de senha.
}

// Função para visualizar o histórico de atividades (simulação)
function viewActivity() {
    alert('Exibindo o histórico de atividades da conta.');
    // Adicione a lógica para exibir o histórico de atividades, como redirecionar para uma página de histórico.
}